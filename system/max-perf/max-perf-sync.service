[Unit]
Description=Sync the max-perf power plan to the caelestia toggle state
After=power-profiles-daemon.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/max-perf-sync

[Install]
WantedBy=multi-user.target
